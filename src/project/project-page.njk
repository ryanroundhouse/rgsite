---
layout: layouts/project-layout.njk
templateClass: tmpl-post
pagination:
  data: projects
  size: 1
  alias: project
permalink: project/{{ project.slug }}/index.html
---
{% block content %}

    <h1>{{ project.title }}</h1> 
    <time datetime="{{ date | dateIso }}">{{ date | dateReadable }}</time>
    <ul>
      {% for tech in project.technologies %}
        <img src="{{tech.logo.url}}" alt="{{tech.logo.alternativeText}}" title="{{tech.logo.alternativeText}}"/>
      {% endfor %}
    </ul>
    <div class="mt-5 bg-white py-3 px-md-5 rounded">
      {% markdown %}{{ project.description | safe }}{% endmarkdown %}  
    </div>

{% endblock %}